{"id": "bat_001", "question": "How many total records are in the dataset?", "expected_sql": "SELECT COUNT(*) AS total_rows FROM battery_cycles", "tags": ["count", "basic"]}
{"id": "bat_002", "question": "How many distinct batteries are in the dataset?", "expected_sql": "SELECT COUNT(DISTINCT battery_id) AS total_batteries FROM battery_cycles", "tags": ["count", "basic"]}
{"id": "bat_003", "question": "What is the maximum cycle number recorded for each battery?", "expected_sql": "SELECT battery_id, MAX(cycle) AS max_cycle FROM battery_cycles GROUP BY battery_id ORDER BY battery_id", "tags": ["aggregation", "group-by"]}
{"id": "bat_004", "question": "On which cycle did B5 first reach End of Life (SOH below 80%)?", "expected_sql": "SELECT MIN(cycle) AS eol_cycle FROM battery_cycles WHERE battery_id = 'B5' AND soh < 80", "tags": ["eol", "filter"]}
{"id": "bat_005", "question": "On which cycle did B7 first reach End of Life (SOH below 80%)?", "expected_sql": "SELECT MIN(cycle) AS eol_cycle FROM battery_cycles WHERE battery_id = 'B7' AND soh < 80", "tags": ["eol", "filter"]}
{"id": "bat_006", "question": "What is the average EOL cycle across all batteries?", "expected_sql": "SELECT AVG(eol_cycle) AS avg_eol FROM (SELECT battery_id, MIN(cycle) AS eol_cycle FROM battery_cycles WHERE soh < 80 GROUP BY battery_id)", "tags": ["eol", "aggregation", "subquery"]}
{"id": "bat_007", "question": "How many records in the dataset have SOH below 80%?", "expected_sql": "SELECT COUNT(*) AS eol_records FROM battery_cycles WHERE soh < 80", "tags": ["filter", "count"]}
{"id": "bat_008", "question": "Which battery has the highest average capacity?", "expected_sql": "SELECT battery_id FROM battery_cycles GROUP BY battery_id ORDER BY AVG(capacity) DESC LIMIT 1", "tags": ["aggregation", "ranking"]}
{"id": "bat_009", "question": "What is the overall average SOH across all records in the dataset?", "expected_sql": "SELECT AVG(soh) AS avg_soh FROM battery_cycles", "tags": ["aggregation", "basic"]}
{"id": "bat_010", "question": "What is the average charge temperature for B5?", "expected_sql": "SELECT AVG(charge_temperature) AS avg_charge_temp FROM battery_cycles WHERE battery_id = 'B5'", "tags": ["filter", "aggregation"]}
{"id": "bat_011", "question": "What is the average SOH for each battery?", "expected_sql": "SELECT battery_id, AVG(soh) AS avg_soh FROM battery_cycles GROUP BY battery_id ORDER BY battery_id", "tags": ["aggregation", "group-by", "soh"]}
{"id": "bat_012", "question": "What is the total number of cycles recorded for each battery?", "expected_sql": "SELECT battery_id, COUNT(*) AS total_cycles FROM battery_cycles GROUP BY battery_id ORDER BY battery_id", "tags": ["aggregation", "group-by"]}
{"id": "bat_013", "question": "What is the minimum SOH value recorded for B7?", "expected_sql": "SELECT MIN(soh) AS min_soh FROM battery_cycles WHERE battery_id = 'B7'", "tags": ["filter", "basic", "soh"]}
{"id": "bat_014", "question": "What is the SOH degradation rate (SOH loss per cycle) for each battery?", "expected_sql": "SELECT battery_id, (MAX(soh) - MIN(soh)) / (MAX(cycle) - MIN(cycle)) AS soh_drop_per_cycle FROM battery_cycles GROUP BY battery_id ORDER BY battery_id", "tags": ["degradation", "aggregation", "computed"]}
{"id": "bat_015", "question": "What is the average discharge temperature for each battery?", "expected_sql": "SELECT battery_id, AVG(discharge_temperature) AS avg_discharge_temp FROM battery_cycles GROUP BY battery_id ORDER BY battery_id", "tags": ["aggregation", "group-by", "temperature"]}
